# requirements.txt (현재 로컬 환경 기준)

# --- FastAPI 서버 및 HTTP 통신 ---
fastapi==0.115.12
uvicorn==0.34.2
python-dotenv==1.1.0
requests==2.32.5
httpx==0.28.1
aiohttp==3.11.18

# --- LangChain & LangGraph (현재 로컬 버전에 맞춤) ---
langchain==0.3.27
langchain-core==0.3.76
langgraph==0.6.8
langchain-openai==0.3.14

# --- 데이터베이스 (Vector Store & Checkpointer) ---
langchain-chroma==0.2.6
aiosqlite==0.21.0
langgraph-checkpoint-sqlite==2.0.11

# --- 기타 프로젝트 핵심 의존성 ---
langchain-mcp-adapters==0.1.10
pandas==2.2.3
numpy==2.2.5
tiktoken==0.9.0
newsapi-python==0.2.7
faiss-cpu==1.12.0

# --- LangGraph 하위 의존성 (명시적 포함) ---
# 이 패키지들은 langgraph의 핵심 기능에 필요합니다.
langgraph-checkpoint==2.1.1
langgraph-prebuilt==0.6.4
langgraph-sdk==0.2.9